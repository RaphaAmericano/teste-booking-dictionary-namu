import { prisma } from "../client";

export class EntryPrismaImplamantation {
  // static async create(data: CreateAuthDto): Promise<any> {
  //   const { password, email, id } = data;
  //   const result = await word.create({
  //     data: {
       
  //   });
  //   return result;
  // }

}
